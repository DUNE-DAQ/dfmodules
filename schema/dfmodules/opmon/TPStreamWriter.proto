syntax = "proto3";

package dunedaq.dfmodules.opmon;

message TPStreamWriterInfo {

  uint64 tpset_received = 1;  // incremental
  uint64 tp_received  = 10;   // incremental
  uint64 tp_written   = 11;   // incremental

}

